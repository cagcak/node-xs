import { Select } from "./decorators";
import { StateMachine } from "./modules";
declare const _default: {
    Store: (state: unknown) => StateMachine<unknown>;
    Select: typeof Select;
};
export = _default;
